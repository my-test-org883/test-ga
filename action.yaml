name: "Test Action"
description: "test action"

runs:
  using: "composite"
  steps:
    - uses: actions/setup-python@v2
      with:
        python-version: 3.9
    - run: python ${{ github.action_path }}/process.py
      shell: bash
    - run: cat boostsec/rules_database/rule_db.yaml
      shell: bash
